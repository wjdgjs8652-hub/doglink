# DOG-LINK 시민 제보자 서비스 제작 프롬프트

당신은 공공기관용 B2G 모바일 웹 서비스 구축 경험이 있는 시니어 프로덕트 디자이너이자 시니어 프론트엔드 엔지니어다.

현재 프로젝트 디렉터리에 포함된 다음 문서를 먼저 읽고, 문서의 명세를 기준으로 시민 제보자 서비스를 구현하라.

- `TalkFile_doglink_제보자_플로우차트_1.md`
- `TalkFile_doglink_제보자_플로우_디자인시스템.md`

두 문서는 참고 자료가 아니라 이번 구현의 핵심 요구사항이다.

문서에 명시된 화면 순서, 분기 규칙, 디자인 토큰, 컴포넌트 규칙, 접근성 원칙을 임의로 삭제하거나 다른 서비스 구조로 변경하지 마라.

구현 전 반드시 현재 코드베이스를 분석하고, 분석 결과와 작업 계획을 먼저 출력한 뒤 코드를 작성하라.

---

## 1. 프로젝트 정의

### 프로젝트명

DOG-LINK  
유기견·유실견 발견 제보 연계 서비스

### 구현 범위

시민이 QR코드 또는 링크로 접속하여 유기견·유실견으로 의심되는 동물을 제보하고, 접수 결과와 처리 상태를 확인하는 모바일 웹 서비스를 구현한다.

이번 작업의 범위는 시민 제보자 서비스다.

다음 항목은 이번 작업 범위에서 제외한다.

- 기관용 관제 대시보드
- 보호소 관리자 페이지
- 지자체 통계 대시보드
- 운영자 로그인과 권한 관리
- 실제 출동 배차 시스템
- 실제 행정기관 내부 업무 시스템

단, 제보 접수와 상태 확인 화면에서 기관 처리 상태를 사용자가 확인할 수 있는 구조는 포함한다.

### 서비스의 핵심 가치

- 앱 설치 없음
- 회원가입 없음
- 로그인 없음
- QR 또는 링크로 즉시 진입
- 사진 한 장으로 제보 시작
- 1분 이내 접수 완료
- 한 화면에서 하나의 질문만 제시
- AI가 사진과 입력 내용을 기반으로 특징을 정리
- AI는 제안하고 담당자가 최종 확인
- 어떤 AI 판정에서도 제보 데이터는 반드시 접수
- 시민에게 불필요한 정보 입력을 요구하지 않음

### 주요 사용자

산책, 출퇴근, 이동 중 유기견 또는 유실견으로 보이는 동물을 우연히 발견한 시민이다.

사용자는 다음 상태일 수 있다.

- 서비스에 대한 사전 지식이 없음
- 당황하거나 급한 상황임
- 제보에 많은 시간을 쓰기 어려움
- 디지털 서비스 사용 능력이 다양함
- 카메라 또는 위치 권한 제공을 거부할 수 있음

따라서 사용자가 설명서를 읽지 않아도 흐름을 이해할 수 있도록 설계하라.

---

## 2. 반드시 구현할 전체 사용자 흐름

다음 순서를 변경하지 마라.

```text
S1 진입
→ S2 사진 촬영 또는 업로드
→ S3 위치 확인
→ S4 목격 내용 선택 입력
→ S5 AI 상태 판정
→ 응급일 경우 S5-E 응급 자동신고 확인
→ S6 접수 완료
→ S7 상태 확인
```

### 분기 규칙

#### AI 판정이 응급인 경우

```text
S5 AI 판정
→ S5-E 응급 자동신고 확인
→ 신고 실행 또는 응급 신고 중단
→ S6 접수 완료
```

사용자가 `응급이 아니에요`를 선택하면 응급 신고만 중단하고 일반 제보로 접수한다.

#### AI 판정이 출동인 경우

```text
S5 AI 판정
→ S6 접수 완료
```

제보는 구조 출동 확인 대상 상태로 접수한다.

#### AI 판정이 부정인 경우

```text
S5 AI 판정
→ 대응 필요성이 낮다는 안내
→ S6 접수 완료
```

부정 판정이어도 제보 기록은 반드시 남긴다.

#### AI 분석 실패 또는 지연

AI 분석이 10초를 초과하거나 오류가 발생하면 판정 결과를 기다리게 하지 말고 일반 제보로 접수한다.

다음 메시지를 제공한다.

> AI 분석이 지연되어 우선 제보를 접수했어요. 담당자가 사진과 내용을 확인합니다.

어떤 오류나 판정에서도 입력한 제보가 사라지는 경로를 만들지 마라.

---

## 3. 공통 화면 구조

S2부터 S5까지의 단계형 화면에는 공통 프레임을 적용한다.

### 상단 영역

- 뒤로가기 버튼
- DOG-LINK 또는 간결한 서비스명
- 현재 진행 상태를 보여주는 StepIndicator
- 불필요한 메뉴, 배너, 광고 금지

### 중앙 영역

- 한 화면에 하나의 핵심 질문
- 한 가지 주요 행동
- 필요한 최소 설명
- 오류와 안내는 해당 입력 요소 가까이에 배치

### 하단 영역

- 모바일 하단 고정 CTA
- 높이 52px
- 화면 좌우 여백 20px
- 하단 safe-area 대응
- 로딩, 비활성화, 오류 상태 제공

### 상태 보존

- 뒤로가기 시 이전 입력값 보존
- 새로고침 시 가능한 범위에서 임시 입력값 보존
- 단계 이동 중 사진, 위치, 선택한 칩, 텍스트가 초기화되지 않도록 처리
- 제출 완료 후에만 임시 작성 데이터를 정리

---

## 4. 화면별 상세 명세

# S1. 제보 진입 화면

QR코드 또는 공유 링크로 접속했을 때 처음 표시되는 화면이다.

### 목표

사용자가 5초 이내에 서비스 목적과 필요한 행동을 이해해야 한다.

### 필수 콘텐츠

헤드라인:

> 유기견을 발견하셨나요?

설명:

> 사진 한 장이면 됩니다. 가입 없이 1분 안에 제보할 수 있어요.

주요 CTA:

> 제보 시작하기

보조 링크:

> 내 제보 상태 확인

### 디자인 규칙

- 헤드라인, 간단한 설명, CTA 중심
- 장식적 요소 최소화
- 광고, 긴 공지, 복잡한 메뉴 금지
- 공공 서비스의 신뢰감과 반려동물 서비스의 따뜻함을 함께 표현
- 상업적인 펫 쇼핑 앱처럼 보이지 않게 구성
- 로그인 또는 회원가입 버튼을 노출하지 않음

---

# S2. 사진 촬영 및 업로드

### 질문

> 동물 사진을 찍어주세요

### 기능

- 모바일에서는 카메라 실행을 우선 시도
- 카메라 사용이 불가능하거나 권한이 거부되면 파일 업로드 제공
- 사진 한 장은 필수
- 추가 사진은 최대 두 장까지 선택 가능
- 전체 최대 세 장
- 촬영 또는 업로드 후 미리보기 표시
- 각 사진의 삭제 및 다시 선택 기능
- 업로드 진행률 표시
- 이미지 압축 또는 리사이징 처리
- 이미지 회전 정보 보정
- 허용되지 않는 형식과 지나치게 큰 파일에 오류 안내

### 가이드 문구

> 멀리서 전체 모습이 보이게 찍으면 AI가 더 정확히 판단해요.

### 주요 CTA

> 이 사진으로 제보

### 상태

- initial
- requesting-permission
- camera-ready
- uploading
- uploaded
- upload-error
- permission-denied

사진이 한 장도 없으면 다음 단계 버튼을 비활성화한다.

오류가 발생해도 사용자가 다시 촬영하거나 파일을 선택할 수 있도록 복구 경로를 제공한다.

---

# S3. 위치 확인

### 질문

> 발견한 위치가 맞나요?

### 기본 흐름

1. 브라우저 위치 권한 요청
2. GPS 좌표 획득
3. 지도 중앙에 위치 핀 표시
4. 사용자가 핀을 드래그하여 위치 조정
5. 좌표를 읽기 쉬운 주소로 변환
6. 사용자가 위치 확정

### 필수 UI

- 최소 높이 280px의 지도
- 중앙 위치 핀
- 현재 주소
- 현재 위치 다시 찾기
- 위치 직접 검색
- 정확한 위치 공개 범위 안내

### 주요 CTA

> 이 위치가 맞아요

### 권한 거부 및 오류 대응

GPS 권한을 거부해도 제보가 중단되지 않아야 한다.

다음 폴백을 제공한다.

- 주소 검색
- 장소명 검색
- 지도 이동 후 핀 지정
- 현재 주소 직접 입력

### 개인정보 안내

원본 좌표는 기관 처리 목적으로 저장할 수 있지만 시민에게 공개되는 상태 확인 페이지에서는 정확한 좌표를 표시하지 않는다.

안내 문구:

> 정확한 위치는 담당 기관에서만 확인하며, 공개 화면에는 주변 범위로 표시됩니다.

---

# S4. 목격 내용 선택 입력

### 질문

> 상황을 알려주시겠어요?  
> 선택 사항이에요.

### 상황 칩

최소 다음 칩을 제공한다.

- 다쳐 보여요
- 움직이지 않아요
- 목줄이 있어요
- 사람을 피해요

다음 칩을 추가할 수 있다.

- 도로 근처에 있어요
- 공격적인 행동을 보여요
- 새끼로 보여요
- 여러 마리가 함께 있어요

### 자유 입력

선택적 textarea를 제공한다.

placeholder 예시:

> 발견 당시 상황이나 동물의 특징을 적어주세요.

### 상호작용

- 상황 칩 다중 선택
- 선택 해제 가능
- 칩만 선택하고 진행 가능
- 아무것도 입력하지 않고 건너뛰기 가능

### 주요 CTA

> 제보 완료하기

### 보조 CTA

> 건너뛰기

건너뛰기는 숨기거나 지나치게 작게 만들지 않는다.

사용자가 선택 입력을 필수로 오해하지 않도록 한다.

---

# S5. AI 상태 판정

제출 직후 AI 분석 상태를 표시한 뒤 판정 결과를 보여준다.

실제 AI API가 연결되지 않은 경우에는 인터페이스와 상태 전이를 검증할 수 있는 mock adapter를 구현하라.

mock 코드를 UI 컴포넌트 내부에 직접 하드코딩하지 말고 서비스 레이어로 분리한다.

## 분석 중 상태

표시 내용:

- 제보 사진 썸네일
- analyzing 상태 TriageBadge
- `AI가 상태를 확인하고 있어요`
- 분석 진행 표시
- 3초 이상 소요 시 추가 안내

3초 초과 안내:

> 조금만 기다려주세요. 사진과 제보 내용을 확인하고 있어요.

10초 초과:

- AI 판정 없이 일반 접수 진행
- 사용자를 계속 대기시키지 않음

pulse 또는 spinner 애니메이션을 사용할 수 있지만 `prefers-reduced-motion`에서는 정지 또는 단순 상태 표시로 대체한다.

## 판정 결과 종류

```ts
type TriageResult =
  | "emergency"
  | "dispatch"
  | "negative"
  | "analyzing"
  | "unavailable";
```

### 응급

즉시 대응이 필요할 가능성이 높은 상태.

표시 문구 예시:

> 응급 상황으로 판단됩니다.

### 출동

담당 기관의 확인 또는 출동이 필요할 가능성이 있는 상태.

표시 문구 예시:

> 담당 기관의 확인이 필요한 것으로 판단됩니다.

### 부정

즉각적인 대응 필요성이 낮아 보이는 상태.

표시 문구 예시:

> 즉각적인 대응이 필요하지 않은 것으로 보입니다.

단, 반드시 다음 안내를 함께 제공한다.

> 제보 기록은 남겨두며 담당자가 최종 확인합니다.

## TriageResultCard 구성

위에서 아래 순서로 배치한다.

1. 제보 사진 썸네일
2. TriageBadge
3. 판정 근거 요약 1~2줄
4. `AI 판단이며 담당자가 최종 확인합니다` 고지
5. `판정이 다른 것 같아요` 수정 진입점

### AI 문구 원칙

단정적으로 표현하지 않는다.

사용 가능:

- 응급 상황으로 판단됩니다.
- 부상이 의심됩니다.
- 움직임이 적어 보입니다.
- 담당 기관의 확인이 필요해 보입니다.

사용 금지:

- 응급입니다.
- 반드시 다쳤습니다.
- 유기견이 확실합니다.
- 구조가 필요 없습니다.

AI가 법적·행정적 최종 결정을 내리는 것처럼 표현하지 마라.

---

# S5-E. 응급 자동신고 확인

응급 판정 시 표시하는 Critical Action 화면이다.

모바일에서는 바텀시트 또는 전체 화면 모달로 구현할 수 있다.

단, 중요한 정보와 버튼이 화면 높이에 의해 잘리지 않도록 한다.

## 필수 구성

### 응급 배너

> 응급 상황으로 판단됩니다

응급 solid 색상을 대면적으로 사용할 수 있는 유일한 화면이다.

### 신고 내용 요약

- 제보 사진
- 발견 위치
- 발견 시간
- 선택한 목격 상황
- AI 판정 근거

### 카운트다운

- 최소 10초
- 숫자와 진행 바를 함께 표시
- 자동 실행 전 남은 시간을 명확하게 안내
- 스크린리더용 `aria-live="assertive"`
- 사용자가 시간을 연장할 수 있는 수단 검토
- 탭이 백그라운드로 이동한 경우 잘못된 즉시 실행이 발생하지 않도록 방어

### 실행 버튼

> 지금 바로 신고

### 중단 버튼

> 응급이 아니에요

중단 버튼은 실행 버튼과 동등한 크기와 충분한 대비를 가져야 한다.

중단 버튼을 링크 형태로 숨기거나, 회색으로 거의 보이지 않게 만들거나, 작은 글씨로 배치하지 마라.

### 중단 시 처리

응급 자동신고만 중단한다.

제보 자체는 일반 접수로 전환하여 S6으로 이동한다.

확인 문구 예시:

> 응급 신고는 중단했어요. 제보 내용은 일반 접수로 전달됩니다.

### 실행 완료 후

> 담당 기관에 전달되었습니다.

접수번호를 함께 표시한다.

완료 후 단순 취소 기능 대신 정정 제보 또는 내용 수정 안내를 제공한다.

---

# S6. 접수 완료

### 필수 콘텐츠

성공 메시지:

> 제보가 접수되었습니다.

감사 문구:

> 소중한 제보가 동물을 찾고 보호하는 데 도움이 됩니다.

감사 문장은 한 줄 또는 두 줄 이내로 절제한다.

### 접수번호

- 고유 접수번호 표시
- 탭하여 복사
- 복사 완료 피드백
- tabular-nums 사용
- 예시: `JJ-4818`

### 처리 타임라인

다음 단계 구조를 사용한다.

1. 제보됨
2. AI 판정
3. 확인 중
4. 기관 전달
5. 출동 또는 보호
6. 반환 또는 종결

현재 단계, 완료 단계, 예정 단계를 구분한다.

각 단계에 상태값과 선택적 타임스탬프를 연결한다.

### 상태 확인 링크

다음 기능을 제공한다.

- 상태 확인 링크 복사
- 브라우저 공유 API 사용
- 공유 API 미지원 시 링크 복사 폴백
- 카카오톡 공유는 SDK가 준비된 경우 adapter 형태로 연결
- 실제 키가 없으면 UI와 인터페이스만 구현하고 키를 임의로 생성하지 않음

### 금지

- 회원가입 유도
- 앱 설치 유도
- 로그인 유도
- 마케팅 수신 동의
- 불필요한 개인정보 입력

---

# S7. 제보 상태 확인

저장한 링크 또는 접수번호로 재방문하는 화면이다.

## 상단 정보

- 접수번호
- TriageBadge
- 현재 처리 StatusBadge
- 접수 일시
- 마지막 갱신 일시

## 처리 타임라인

- 각 단계별 상태
- 처리된 단계의 타임스탬프
- 현재 단계 강조
- 예정 단계 비활성 표현
- 응급 신고 건의 중요한 시점은 초 단위까지 표시 가능

## 위치 표시

- 정확한 핀을 공개하지 않음
- 반경 원 또는 지역명으로 표시
- 위치 범위가 정확한 발견 지점이 아니라는 안내 제공

안내 문구:

> 동물 보호와 제보자 개인정보 보호를 위해 발견 위치는 주변 범위로 표시됩니다.

## 상태 안내

> 상태가 바뀌면 이 페이지에서 확인할 수 있어요.

알림 기능이 실제로 구현되지 않았다면 푸시 알림이나 문자 발송을 제공하는 것처럼 표현하지 마라.

## 오류 상태

- 존재하지 않는 접수번호
- 만료되거나 잘못된 링크
- 네트워크 오류
- 상태 불러오기 실패

오류가 발생하면 다음 행동을 제공한다.

- 다시 시도
- 접수번호 재입력
- 홈으로 이동

---

## 5. 디자인 방향

전체 서비스는 다음 인상을 가져야 한다.

- 공공기관 서비스
- 안정적
- 신뢰할 수 있음
- 차분함
- 따뜻하지만 지나치게 귀엽지 않음
- 상업적이지 않음
- 빠르게 이해할 수 있음
- 위기 상황에서도 조작하기 쉬움

### 금지 스타일

- 쇼핑몰형 펫 앱
- 캐릭터와 장식이 지나치게 많은 화면
- 과도한 그라데이션
- 유리 질감과 네온 효과
- 과도한 그림자
- 작은 회색 글씨
- 카드가 지나치게 많은 대시보드
- 중요한 기능을 아이콘만으로 표시
- AI 기능을 과장하는 미래지향적 연출
- 응급 상황을 공포스럽게 표현하는 이미지

---

## 6. 디자인 토큰

다음 토큰을 프로젝트의 전역 디자인 토큰으로 등록한다.

프로젝트가 Tailwind CSS를 사용한다면 theme에 semantic naming으로 등록한다.

일반 CSS 또는 CSS Modules를 사용한다면 `:root` 변수로 등록한다.

```css
:root {
  /* Background */
  --color-background: #F8F9F8;
  --color-surface: #FFFFFF;
  --color-surface-subtle: #F2F4F2;
  --color-surface-muted: #E9EDEA;

  /* Text */
  --color-text-primary: #10110E;
  --color-text-secondary: #5F6863;
  --color-text-tertiary: #8C9590;
  --color-text-disabled: #AEB5B1;
  --color-text-inverse: #FFFFFF;

  /* Border */
  --color-border-default: #D8DDDA;
  --color-border-strong: #B8C0BC;
  --color-divider: #DDE2DF;

  /* Primary */
  --color-primary-50: #EEF1FC;
  --color-primary-100: #DDE4FA;
  --color-primary-500: #526ED8;
  --color-primary-600: #425BBE;
  --color-primary-700: #35499D;

  /* Triage: emergency */
  --triage-emergency-text: #B01E1E;
  --triage-emergency-bg: #FDEBEB;
  --triage-emergency-border: #E89B9B;
  --triage-emergency-solid: #D32F2F;

  /* Triage: dispatch */
  --triage-dispatch-text: #9A5A10;
  --triage-dispatch-bg: #FFF3D3;
  --triage-dispatch-border: #F2D58A;

  /* Triage: negative */
  --triage-negative-text: #59615D;
  --triage-negative-bg: #EEF0EF;
  --triage-negative-border: #D3D8D5;

  /* Triage: analyzing */
  --triage-analyzing-text: #35499D;
  --triage-analyzing-bg: #EEF1FC;
  --triage-analyzing-border: #B9C6F0;

  /* Processing status */
  --status-pending-text: #9A5A10;
  --status-pending-bg: #FFF3D3;
  --status-pending-border: #F2D58A;

  --status-success-text: #167A42;
  --status-success-bg: #E8F7EE;
  --status-success-border: #A7DCB9;

  --status-transfer-text: #743A99;
  --status-transfer-bg: #F2E8F8;
  --status-transfer-border: #D4B5E6;

  --status-danger-text: #B83232;
  --status-danger-bg: #FDECEC;
  --status-danger-border: #EAB2B2;

  --status-neutral-text: #59615D;
  --status-neutral-bg: #EEF0EF;
  --status-neutral-border: #D3D8D5;

  /* Spacing */
  --space-1: 4px;
  --space-2: 8px;
  --space-3: 12px;
  --space-4: 16px;
  --space-5: 20px;
  --space-6: 24px;
  --space-8: 32px;
  --space-10: 40px;

  /* Radius */
  --radius-small: 8px;
  --radius-medium: 12px;
  --radius-large: 16px;
  --radius-xlarge: 20px;
  --radius-pill: 999px;
}
```

### 색상 적용 규칙

- primary: 일반적인 주요 행동
- emergency: 응급 가능성
- dispatch: 기관 확인 또는 출동 필요
- negative: 대응 필요성이 낮음
- analyzing: AI 분석 중
- success: 보호, 전달 완료, 처리 완료
- transfer: 기관 전달
- neutral: 예정, 비활성, 종결

트리아지 상태는 항상 다음 세 요소를 함께 사용한다.

1. 색상
2. 아이콘
3. 한글 라벨

색상만으로 의미를 전달하지 마라.

`--triage-emergency-solid`는 S5-E 응급 확인 화면 이외에서 넓은 면적으로 사용하지 않는다.

---

## 7. 타이포그래피

```css
font-family:
  Pretendard,
  SUIT,
  "Noto Sans KR",
  "Apple SD Gothic Neo",
  system-ui,
  sans-serif;
```

### 타입 스케일

```text
display:      24px / 32px / 700
title-page:   20px / 28px / 700
title-card:   15px / 22px / 600
body:         14px / 22px / 400
body-strong:  14px / 22px / 600
label:        13px / 18px / 600
caption:      12px / 18px / 400
button:       14px / 20px / 600
mono-id:      13px / 18px / 500
```

### 규칙

- 중요한 텍스트에 12px 미만 사용 금지
- 본문은 14px 이상
- 제목 700
- 강조 600
- 일반 본문 400
- 접수번호에는 tabular-nums 적용
- 한 화면에서 굵기 종류를 과도하게 사용하지 않음

---

## 8. 레이아웃 규칙

### 모바일 우선

기본 지원 너비:

- 360px
- 375px
- 393px
- 402px
- 430px

### 기본 여백

- 화면 좌우: 20px
- 카드 내부: 16px
- 카드 간격: 12px
- 섹션 간격: 32~40px
- 버튼 높이: 최소 44px
- 주요 하단 CTA: 52px
- 지도 모서리: 20px
- 일반 카드 모서리: 16px
- 버튼 모서리: 12px
- 배지 모서리: pill

### 그림자

- 기본 카드에는 그림자 사용 금지
- 테두리로 영역 구분
- 바텀시트와 팝오버에만 약한 그림자 사용

### 점선

실제 서비스 UI에서 점선 테두리를 선택, 강조, 오류 상태에 사용하지 않는다.

---

## 9. 필수 공통 컴포넌트

다음 컴포넌트를 재사용 가능한 형태로 구현한다.

### AppHeader

```ts
interface AppHeaderProps {
  title?: string;
  showBack?: boolean;
  onBack?: () => void;
}
```

### StepIndicator

```ts
type StepState = "completed" | "current" | "upcoming";

interface StepItem {
  id: string;
  label: string;
  state: StepState;
}
```

규칙:

- 완료: primary 채움
- 현재: primary 강조
- 예정: neutral
- 좁은 화면에서 단계명이 겹치지 않도록 처리
- 스크린리더에 현재 단계 정보 제공

### BottomCTA

```ts
interface BottomCTAProps {
  label: string;
  disabled?: boolean;
  loading?: boolean;
  onClick?: () => void;
}
```

- 높이 52px
- safe-area 대응
- 로딩 상태에서 중복 제출 방지
- 비활성 이유를 사용자가 이해할 수 있어야 함

### PhotoCapture

- 카메라 실행
- 업로드 폴백
- 최대 세 장
- 진행률
- 프리뷰
- 삭제
- 재촬영
- 오류 복구

### LocationPicker

- GPS
- 지도
- 핀 이동
- 주소 변환
- 검색 폴백
- 권한 오류 안내

### SituationChips

```ts
interface SituationChip {
  id: string;
  label: string;
}
```

- 다중 선택
- 높이 최소 40px
- 전체 터치 영역 44px 확보
- 선택 상태는 배경, 테두리, 체크 상태로 표시

### TriageBadge

```ts
type TriageVariant =
  | "emergency"
  | "dispatch"
  | "negative"
  | "analyzing";
```

- 기본 높이 28px
- 대형 32px
- 아이콘 14px
- 한글 라벨 필수

### TriageResultCard

필수 항목:

- 사진
- 판정 배지
- 근거 요약
- AI 고지
- 수정 진입점

### EmergencyConfirmSheet

필수 항목:

- 응급 배너
- 신고 내용
- 카운트다운
- 즉시 신고
- 응급 신고 중단
- 접근성 안내

### ProcessTimeline

```ts
type ProcessStepStatus =
  | "completed"
  | "current"
  | "upcoming";

interface ProcessStep {
  id: string;
  label: string;
  status: ProcessStepStatus;
  timestamp?: string;
}
```

- 현재, 완료, 예정 구분
- 모바일에서도 라벨 겹침 방지
- 상태 확인 화면에서는 타임스탬프 표시

### StatusBadge

처리 상태와 AI 트리아지 상태를 하나의 variant로 혼합하지 마라.

`TriageBadge`와 `StatusBadge`를 별도 컴포넌트로 유지한다.

### InlineNotice

종류:

- info
- warning
- success
- error

권한 거부, 위치 공개 범위, AI 고지, 네트워크 오류에 사용한다.

---

## 10. 데이터 모델

실제 백엔드 명세가 현재 코드베이스에 없다면 아래 인터페이스를 임시 기준으로 사용하되, API adapter를 통해 교체 가능하게 만든다.

```ts
interface ReporterDraft {
  photos: ReportPhoto[];
  location: ReportLocation | null;
  situations: string[];
  description: string;
}

interface ReportPhoto {
  id: string;
  localUrl?: string;
  remoteUrl?: string;
  status: "idle" | "uploading" | "uploaded" | "error";
  progress?: number;
}

interface ReportLocation {
  latitude: number;
  longitude: number;
  address: string;
  source: "gps" | "search" | "manual";
}

type TriageType =
  | "emergency"
  | "dispatch"
  | "negative"
  | "unavailable";

interface TriageAssessment {
  type: TriageType;
  summary: string;
  analyzedAt?: string;
  isHumanReviewed: boolean;
}

type ProcessingStatus =
  | "submitted"
  | "triaged"
  | "reviewing"
  | "transferred"
  | "dispatched"
  | "protected"
  | "returned"
  | "closed";

interface PublicReport {
  reportId: string;
  submittedAt: string;
  updatedAt: string;
  triage: TriageAssessment;
  processingStatus: ProcessingStatus;
  timeline: ProcessStep[];
  publicLocationLabel: string;
  publicLocationRadiusMeters?: number;
}
```

### 개인정보 최소화

시민 제보 플로우에서 다음 정보를 기본적으로 요구하지 않는다.

- 이름
- 전화번호
- 주민등록번호
- 생년월일
- 상세 주소
- 회원 계정
- 이메일

연락처가 반드시 필요한 요구사항이 코드베이스에 이미 존재한다면 선택 입력으로 만들고, 사용 목적과 보관 기준을 명확히 표시한다.

문서에 없는 개인정보 수집 필드를 임의로 추가하지 마라.

---

## 11. 라우팅 제안

현재 프로젝트 구조와 라우터를 우선 확인한다.

라우팅이 아직 없다면 다음 구조를 사용할 수 있다.

```text
/                         S1 진입
/report/photo             S2 사진
/report/location          S3 위치
/report/details           S4 목격 내용
/report/triage            S5 AI 판정
/report/complete/:id      S6 접수 완료
/report/status/:id        S7 상태 확인
/design-system            컴포넌트 미리보기
```

S5-E는 `/report/triage` 내부의 바텀시트 또는 모달 상태로 처리할 수 있다.

브라우저 뒤로가기가 실제 단계 이동과 일치하도록 구현한다.

페이지 이동과 전역 상태가 불일치하지 않도록 한다.

---

## 12. 상태 관리

현재 프로젝트에서 이미 사용하는 상태 관리 방식을 우선 사용한다.

새로운 전역 상태 라이브러리를 무조건 설치하지 마라.

필요하다면 다음 중 프로젝트에 가장 적합한 방식을 선택한다.

- React Context
- Zustand
- 기존 상태 관리 라이브러리
- 라우터와 sessionStorage 조합

### 필수 조건

- 뒤로가기 시 입력값 보존
- 사진 미리보기 유지
- 위치 좌표 유지
- 선택 칩 유지
- 새로고침 시 임시 복원 가능
- 제출 중 중복 요청 방지
- 접수 성공 이후 상태 초기화
- 민감한 위치 데이터는 불필요하게 장기간 localStorage에 남기지 않음

---

## 13. API 및 mock 처리

현재 프로젝트에 실제 API가 있는지 먼저 확인한다.

### 실제 API가 있다면

- 기존 API 규격 사용
- API 호출 코드를 UI에서 분리
- 오류와 재시도 처리
- 요청 중복 방지
- 타입 정의
- 환경 변수 사용

### 실제 API가 없다면

다음 adapter를 구성한다.

```text
services/
  report-service
  upload-service
  geolocation-service
  map-service
  triage-service
  share-service
```

mock 응답은 실제 한국어 데이터 형태로 작성한다.

예시:

```text
발견 위치: 제주특별자치도 제주시 노형동 공원 인근
특징: 갈색 소형견, 목줄 착용
판정 근거: 왼쪽 뒷다리 부상 가능성이 있으며 움직임이 적어 보입니다.
접수번호: JJ-4818
```

Lorem ipsum과 의미 없는 영문 샘플을 사용하지 마라.

실제 행정기관에 신고가 전송되는 것처럼 속이지 마라.

mock 환경에서는 다음과 같이 명확히 분리한다.

```ts
const isMockMode = true;
```

UI에는 개발용 표시를 과도하게 노출하지 않되, 개발 문서에는 mock임을 기록한다.

---

## 14. 지도 구현

현재 코드베이스에 지도 SDK가 있다면 해당 SDK를 유지한다.

새로 구현해야 한다면 Kakao Maps 또는 Naver Maps를 adapter 구조로 연결한다.

### 필수 고려 사항

- API 키는 환경 변수 사용
- 키를 소스 코드에 하드코딩하지 않음
- SDK 로드 실패 폴백
- 지도 없이도 주소 검색과 수동 위치 입력 가능
- 위치 권한 없이도 전체 제보 완료 가능
- S3에서는 정확 좌표 사용 가능
- S7 공개 화면에서는 위치 범위만 표시

API 키가 제공되지 않았다면 빈 지도 화면을 방치하지 말고 mock map 또는 위치 선택 fallback UI를 제공한다.

---

## 15. 접근성

WCAG 2.1 AA를 목표로 한다.

### 필수 조건

- 일반 텍스트 대비 4.5:1 이상
- 큰 텍스트 대비 3:1 이상
- 터치 영역 최소 44×44px
- 모든 인터랙션 키보드 접근 가능
- `focus-visible` 제공
- 아이콘 버튼에 `aria-label`
- 입력 요소와 label 연결
- 오류 메시지와 입력 요소 연결
- 현재 단계에 `aria-current="step"`
- 선택 칩에 `aria-pressed`
- 로딩 상태에 `aria-busy`
- 동적 상태 안내에 적절한 `aria-live`
- 응급 카운트다운에 `aria-live="assertive"`
- 색상 외 아이콘과 텍스트로 상태 구분
- `prefers-reduced-motion` 지원
- 카메라와 GPS 권한 거부 폴백
- 하단 CTA가 모바일 safe-area를 침범하지 않음

응급 화면에서 자동 동작을 숨기지 말고, 사용자가 현재 발생할 행동과 남은 시간을 인식할 수 있게 한다.

---

## 16. 오류 및 빈 상태

각 단계에 다음 오류 상태를 구현한다.

### 공통

- 네트워크 연결 실패
- 서버 응답 지연
- 예상하지 못한 오류
- 중복 제출
- 이전 단계 데이터 누락

### 사진

- 권한 거부
- 카메라 미지원
- 업로드 실패
- 파일 형식 오류
- 파일 크기 초과

### 위치

- GPS 권한 거부
- 위치 획득 시간 초과
- 주소 변환 실패
- 지도 SDK 로드 실패

### AI 판정

- 분석 지연
- 분석 실패
- 잘못된 응답
- 네트워크 종료

### 상태 확인

- 잘못된 접수번호
- 존재하지 않는 제보
- 만료된 링크
- 데이터 불러오기 실패

오류 메시지는 사용자의 잘못처럼 표현하지 않는다.

나쁜 예:

> 올바르게 입력하지 않았습니다.

좋은 예:

> 위치를 불러오지 못했어요. 주소를 검색해서 직접 지정할 수 있습니다.

모든 오류에는 다음 행동을 제공한다.

- 다시 시도
- 다른 방법 사용
- 이전 단계로 이동
- 일반 접수로 계속 진행

---

## 17. 보안 및 개인정보

- 클라이언트에 비밀 API 키 저장 금지
- 업로드 파일 형식과 크기 검증
- 사용자 입력 출력 시 XSS 방지
- 접수번호를 단순 증가 숫자로 만들지 않음
- 공개 상태 URL에서 정확한 위치 좌표 노출 금지
- 사진 EXIF 위치 정보 제거 또는 서버 정책에 맞게 처리
- 콘솔에 위치 정보와 사진 URL을 불필요하게 출력하지 않음
- 실제 기관 신고 API가 없다면 외부 전송을 흉내 내지 않음
- 브라우저 저장소에 위치와 사진을 장기간 보존하지 않음

---

## 18. 반응형 구현

### 360~430px

핵심 모바일 화면이다.

- 한 열
- 좌우 20px
- 하단 CTA 고정
- 가로 넘침 금지
- 키보드가 열려도 입력과 CTA 접근 가능
- 지도와 사진 프리뷰가 화면 밖으로 넘치지 않음

### 768px 이상

- 콘텐츠 최대 너비를 제한
- 입력 폼을 무리하게 2열로 만들지 않음
- 지도와 보조 설명을 상황에 따라 2열로 구성 가능

### 1024px 이상

- 모바일 폼을 화면 전체로 늘리지 않음
- 중앙 콘텐츠 최대 너비 약 560~640px
- 필요하면 우측에 제보 과정 안내를 배치할 수 있으나, 새로운 기능이나 광고는 추가하지 않음

---

## 19. 코드 품질 지침

### 반드시 지킬 것

- TypeScript 사용 시 `any` 최소화
- 컴포넌트 props 타입 명시
- 상태 variant 타입 명시
- 반복되는 UI는 공통 컴포넌트로 분리
- 디자인 토큰 사용
- API와 UI 분리
- mock과 production adapter 분리
- 접근성 속성 포함
- 로딩과 오류 상태 포함
- 기존 코드 스타일과 폴더 구조 존중

### 금지

- 기존 프레임워크 임의 교체
- 불필요한 라이브러리 설치
- 모든 코드를 하나의 페이지에 작성
- 색상과 간격 하드코딩
- 화면마다 별도의 Badge 구현
- 인라인 스타일 남용
- 실제 API 키 임의 작성
- 존재하지 않는 백엔드 기능을 구현된 것처럼 표현
- 문서에 없는 회원가입, 로그인, 마케팅 기능 추가
- AI 판정 결과를 확정적 사실로 표현
- 응급 중단 버튼을 의도적으로 약하게 표현
- 오류 시 제보 데이터 폐기

---

## 20. 구현 순서

다음 순서로 작업한다.

### 1단계: 코드베이스 분석

확인할 내용:

- 프레임워크
- 라우팅
- 스타일링 방식
- 상태 관리
- 기존 컴포넌트
- 지도 SDK
- API 구조
- 환경 변수
- 테스트 도구

분석 결과를 먼저 출력한다.

### 2단계: 작업 계획

다음을 제시한다.

- 재사용할 기존 코드
- 새로 만들 파일
- 수정할 파일
- 라우팅 구조
- 상태 관리 방식
- mock 또는 API 전략
- 접근성 전략

### 3단계: 디자인 토큰

- 색상
- 타이포그래피
- 간격
- 모서리
- 상태 색상
- 전역 스타일

### 4단계: 공통 컴포넌트

우선 구현:

1. AppHeader
2. StepIndicator
3. BottomCTA
4. Button variants
5. TriageBadge
6. StatusBadge
7. InlineNotice
8. ProcessTimeline

### 5단계: 입력 플로우

1. S1 진입
2. S2 사진
3. S3 위치
4. S4 선택 입력

### 6단계: AI 판정

1. analyzing
2. emergency
3. dispatch
4. negative
5. timeout fallback
6. 판정 수정 진입점

### 7단계: 응급 확인

- 바텀시트
- 카운트다운
- 즉시 신고
- 응급 신고 중단
- 일반 접수 전환

### 8단계: 완료 및 상태 확인

- 접수번호
- 링크 복사
- 공유
- 타임라인
- 공개 위치 범위

### 9단계: 디자인 시스템 페이지

`/design-system` 페이지에 다음 상태를 표시한다.

- 버튼 전체 variant
- TriageBadge 전체 상태
- StatusBadge 전체 상태
- StepIndicator 상태
- SituationChips 선택/미선택
- InlineNotice 종류
- BottomCTA 로딩/비활성
- ProcessTimeline 진행 상태
- 오류 메시지 예시

### 10단계: 문서화

다음 파일을 생성한다.

```text
docs/design-system.md
docs/reporter-flow.md
docs/mock-api.md
```

---

## 21. 테스트 및 검수

가능한 테스트 환경이 있다면 다음 테스트를 작성하거나 수동 검수 결과를 기록한다.

### 핵심 사용자 흐름

- S1부터 S6까지 정상 완료
- 사진 촬영 권한 거부 후 업로드로 완료
- 위치 권한 거부 후 주소 검색으로 완료
- S4 건너뛰기
- 응급 판정 후 즉시 신고
- 응급 판정 후 `응급이 아니에요`
- 출동 판정
- 부정 판정
- AI 분석 10초 초과 폴백
- 상태 확인 링크 재진입
- 잘못된 접수번호

### 입력 보존

- S4에서 S3로 뒤로 이동 후 위치 유지
- S3에서 S2로 이동 후 사진 유지
- 새로고침 후 임시 입력 복원
- 제출 성공 후 임시 입력 제거

### 반응형

- 360px
- 375px
- 393px
- 402px
- 430px
- 768px
- 1024px 이상

### 접근성

- 키보드만으로 전체 진행
- 스크린리더용 라벨
- 포커스 순서
- 색상 없이 상태 구분
- 응급 카운트다운 안내
- reduced motion
- 200% 확대 시 사용 가능

---

## 22. 최종 완료 기준

아래 조건을 모두 충족해야 완료로 간주한다.

- [ ] QR 또는 링크 진입을 전제로 한 첫 화면이 구현됨
- [ ] 로그인과 회원가입 없이 제보 가능
- [ ] 사진 한 장으로 제보 시작 가능
- [ ] 사진은 최대 세 장
- [ ] GPS 권한 없이도 위치 지정 가능
- [ ] 목격 내용은 완전한 선택 사항
- [ ] AI 분석 상태와 세 가지 판정 결과 구현
- [ ] AI 판정 근거와 담당자 최종 확인 고지 표시
- [ ] 판정 수정 진입점 제공
- [ ] AI 분석 10초 초과 시 일반 접수
- [ ] 모든 판정에서 제보가 접수됨
- [ ] 응급 확인 카운트다운 최소 10초
- [ ] 즉시 신고와 응급 중단 버튼의 접근성이 동등함
- [ ] 응급 중단 후 일반 제보로 접수됨
- [ ] 접수번호 복사 가능
- [ ] 상태 확인 링크 저장 또는 공유 가능
- [ ] 처리 타임라인 표시
- [ ] 공개 화면에서 정확한 위치 좌표가 숨겨짐
- [ ] 360px에서 가로 넘침 없음
- [ ] 하단 CTA가 safe-area를 침범하지 않음
- [ ] 뒤로가기 시 입력값 보존
- [ ] 12px 미만 중요 텍스트 없음
- [ ] 44×44px 미만 핵심 터치 영역 없음
- [ ] 트리아지 상태가 색상 없이도 구분됨
- [ ] solid 응급색이 S5-E 밖에서 남용되지 않음
- [ ] 디자인이 상업용 펫 앱보다 공공 서비스로 인식됨
- [ ] mock 기능과 실제 연동 기능이 명확히 분리됨
- [ ] 변경 파일과 구현 내용을 문서화함

---

## 23. Claude Code 응답 형식

작업을 바로 시작하되, 다음 순서로 응답하라.

### A. 코드베이스 분석

현재 프로젝트의 구조와 사용 기술을 요약한다.

### B. 구현 계획

수정 및 생성할 파일과 작업 순서를 제시한다.

### C. 확인이 필요한 사항

구현을 막는 치명적인 정보가 있을 때만 질문한다.

지도 API 키, 실제 백엔드 주소처럼 제공되지 않은 값은 임의로 만들지 말고 mock 또는 adapter로 처리한다.

### D. 코드 구현

계획에 따라 실제 파일을 생성하고 수정한다.

### E. 검수

빌드, 타입 검사, 린트, 가능한 테스트를 실행한다.

### F. 완료 보고

다음을 정리한다.

- 생성한 화면
- 생성한 컴포넌트
- 수정한 파일
- mock 처리된 기능
- 실제 연동이 필요한 기능
- 접근성 처리
- 남은 제한 사항

불확실한 부분은 임의로 새로운 기능을 추가하지 말고, 업로드된 두 명세 문서와 현재 코드베이스를 기준으로 가장 보수적으로 구현하라.
