# 유기견 발견 제보 연계 서비스 디자인 시스템 프롬프트

## 화면 분석 요약

이 화면은 **공공기관용 유기견 제보·처리 현황 서비스**라는 목적에 맞게 정보량은 충분하지만, 현재는 카드·배지·버튼의 규칙이 조금씩 다르고 하단으로 갈수록 정보가 조밀합니다.

디자인 시스템에서는 다음 방향으로 정리합니다.

- 귀여운 반려동물 앱보다 **신뢰도 높은 공공 서비스**
- 파란색은 주요 행동, 초록색은 보호·반환 완료 등 **상태 색상 역할 고정**
- 카드, 버튼, 배지, 구분선, 여백을 동일한 토큰으로 통일
- 작은 글씨와 작은 버튼을 확대해 접근성 개선
- 보라색 점선 테두리는 실제 UI에서 제거
- 모바일 웹을 기준으로 설계하되 PC 웹까지 자연스럽게 확장

---

# Claude Code용 디자인 시스템 프롬프트

당신은 공공기관·행정 서비스에 특화된 시니어 UI/UX 디자이너이자 프론트엔드 디자인 시스템 엔지니어다.

첨부한 모바일 메인 화면을 분석하여, 현재 프로젝트 전체에 적용할 수 있는 일관된 디자인 시스템을 설계하고 코드에 반영하라.

---

## 1. 프로젝트 개요

### 프로젝트명

유기견 발견 제보 연계 서비스

### 서비스 성격

- 시민이 길에서 유기견·유실견으로 의심되는 동물을 발견했을 때 제보하는 공공 서비스
- 앱 설치 없이 QR코드 또는 링크를 통해 웹으로 접속
- 시민용 모바일 웹과 공공기관·운영자용 PC 웹을 함께 제공
- 민간 반려동물 쇼핑 앱이나 커뮤니티 앱이 아닌 B2G 공공 서비스
- 주요 사용자는 시민, 보호소 담당자, 지자체 담당자, 구조기관 운영자

### 핵심 기능

- 실시간 구조 현황 확인
- 주변 유기견 제보 확인
- 지도 기반 제보 위치 확인
- 거리순·최근순 정렬
- 제보 상태 확인
- 기관 전달 및 보호·반환 처리
- 운영자 처리 단계 변경
- 처리 소요시간 및 진행 현황 표시

### 디자인 목표

- 시민이 처음 접속해도 5초 이내에 핵심 정보를 이해할 수 있어야 한다.
- 제보 상태와 처리 단계가 명확하게 구분되어야 한다.
- 공공기관 서비스에 필요한 신뢰감, 안정감, 접근성을 확보한다.
- 반려동물 서비스의 따뜻함은 유지하되 지나치게 귀엽거나 상업적으로 보이지 않도록 한다.
- 모바일과 PC에서 동일한 디자인 언어를 사용한다.

---

## 2. 기존 화면 분석

첨부 화면의 긍정적인 요소는 유지한다.

### 유지할 요소

- 흰색과 밝은 회색을 중심으로 한 정돈된 화면
- 파란색을 이용한 주요 행동 및 진행 단계 표시
- 초록색, 노란색, 보라색을 이용한 상태 구분
- 둥근 카드와 상태 배지
- 실시간 구조 현황 카드
- 제주 지도와 제보 위치 표시
- 제보 목록과 운영자 처리 단계
- 단계별 진행 타임라인
- 공공 서비스에 적합한 간결한 레이아웃

### 개선할 문제

1. 카드마다 테두리, 모서리, 내부 여백이 조금씩 다르다.
2. 상태 배지의 높이, 색상, 글자 크기, 좌우 여백이 일정하지 않다.
3. 일부 글자가 지나치게 작아 접근성이 떨어진다.
4. 파란색, 초록색, 노란색, 보라색의 역할이 명확히 고정되어 있지 않다.
5. 지도 범례와 보조 정보의 가독성이 낮다.
6. 하단 운영자 처리 영역의 정보 밀도가 높고 위계가 약하다.
7. 버튼의 높이가 작고 터치 영역이 충분하지 않은 부분이 있다.
8. 보라색 점선 테두리는 개발·선택 표시처럼 보이므로 실제 서비스 UI에서는 제거한다.
9. 여러 종류의 구분선이 혼용되고 있다.
10. 상태 정보와 일반 설명 정보가 시각적으로 충분히 구분되지 않는다.

기존 구조와 기능은 함부로 삭제하지 말고, 디자인 규칙을 통일하는 방식으로 개선한다.

---

## 3. 디자인 원칙

다음 다섯 가지 원칙을 모든 화면과 컴포넌트에 적용한다.

### 1. Public Trust

공공기관 서비스처럼 정확하고 안정적으로 보여야 한다.

### 2. Status First

제보의 현재 상태와 다음 행동을 가장 먼저 인식할 수 있어야 한다.

### 3. Calm and Clear

과도한 장식, 그림자, 그라데이션을 사용하지 않는다.

### 4. Accessible by Default

작은 글자, 낮은 명도 대비, 작은 터치 영역을 금지한다.

### 5. Mobile First, Desktop Ready

모바일에서 먼저 완성하고 태블릿과 PC로 자연스럽게 확장한다.

---

## 4. 컬러 디자인 토큰

CSS 변수 또는 프로젝트에서 사용하는 토큰 시스템으로 다음 색상을 정의한다.

### 기본 배경

```css
--color-background: #F8F9F8;
--color-surface: #FFFFFF;
--color-surface-subtle: #F2F4F2;
--color-surface-muted: #E9EDEA;
```

### 텍스트

```css
--color-text-primary: #10110E;
--color-text-secondary: #5F6863;
--color-text-tertiary: #8C9590;
--color-text-disabled: #AEB5B1;
--color-text-inverse: #FFFFFF;
```

### 테두리와 구분선

```css
--color-border-default: #D8DDDA;
--color-border-strong: #B8C0BC;
--color-divider: #DDE2DF;
```

### 주요 브랜드 색상

```css
--color-primary-50: #EEF1FC;
--color-primary-100: #DDE4FA;
--color-primary-500: #526ED8;
--color-primary-600: #425BBE;
--color-primary-700: #35499D;
```

주요 행동 버튼과 선택된 상태에는 `primary-500`을 사용한다.  
호버에는 `primary-600`, 눌림에는 `primary-700`을 사용한다.

### 상태 색상

#### 확인 중 / 접수 대기

```css
--color-status-pending-text: #9A5A10;
--color-status-pending-bg: #FFF3D3;
--color-status-pending-border: #F2D58A;
```

#### 보호 / 반환

```css
--color-status-success-text: #167A42;
--color-status-success-bg: #E8F7EE;
--color-status-success-border: #A7DCB9;
```

#### 기관 전달

```css
--color-status-transfer-text: #743A99;
--color-status-transfer-bg: #F2E8F8;
--color-status-transfer-border: #D4B5E6;
```

#### 긴급 / 오류

```css
--color-status-danger-text: #B83232;
--color-status-danger-bg: #FDECEC;
--color-status-danger-border: #EAB2B2;
```

#### 처리 완료 / 비활성

```css
--color-status-neutral-text: #59615D;
--color-status-neutral-bg: #EEF0EF;
--color-status-neutral-border: #D3D8D5;
```

상태 색상은 장식이 아닌 의미 전달에만 사용한다.  
색상만으로 상태를 전달하지 말고 텍스트, 아이콘 또는 점 표시를 함께 사용한다.

---

## 5. 타이포그래피

한국어 가독성이 좋은 산세리프 글꼴을 사용한다.

### 권장 순서

```css
font-family:
  Pretendard,
  SUIT,
  "Noto Sans KR",
  "Apple SD Gothic Neo",
  system-ui,
  sans-serif;
```

### 타입 스케일

| 유형 | 크기 | 줄 높이 | 굵기 |
|---|---:|---:|---:|
| Display | 24px | 32px | 700 |
| Page Title | 20px | 28px | 700 |
| Section Title | 18px | 26px | 700 |
| Card Title | 15px | 22px | 600 |
| Body | 14px | 22px | 400 |
| Body Strong | 14px | 22px | 600 |
| Label | 13px | 18px | 600 |
| Caption | 12px | 18px | 400 |
| Button | 14px | 20px | 600 |

### 원칙

- 의미 있는 정보에 12px 미만 글자를 사용하지 않는다.
- 본문은 기본적으로 14px 이상 사용한다.
- 숫자와 사건 번호는 동일한 방식으로 표시한다.
- 지나치게 많은 굵기 단계를 사용하지 않는다.
- 제목은 700, 강조는 600, 일반 문장은 400을 기본으로 한다.

---

## 6. 간격 시스템

4px 기반 간격 시스템을 사용한다.

```css
--space-1: 4px;
--space-2: 8px;
--space-3: 12px;
--space-4: 16px;
--space-5: 20px;
--space-6: 24px;
--space-8: 32px;
--space-10: 40px;
--space-12: 48px;
```

### 사용 규칙

- 모바일 화면 좌우 기본 여백: 20px
- 카드 내부 기본 여백: 16px
- 섹션과 섹션 사이: 32~40px
- 제목과 콘텐츠 사이: 12~16px
- 카드와 카드 사이: 12px
- 아이콘과 텍스트 사이: 6~8px
- 배지 내부 좌우 여백: 10~12px

임의의 7px, 13px, 19px 같은 값을 만들지 말고 토큰을 사용한다.

---

## 7. 모서리와 테두리

```css
--radius-small: 8px;
--radius-medium: 12px;
--radius-large: 16px;
--radius-xlarge: 20px;
--radius-pill: 999px;
```

### 적용

- 작은 입력 필드와 보조 버튼: 10~12px
- 일반 카드: 16px
- 지도 및 주요 패널: 20px
- 상태 배지: pill
- 이미지 상단 모서리: 카드 반경과 동일

### 테두리

- 기본 카드: `1px solid var(--color-border-default)`
- 강조 카드: `1px solid var(--color-primary-500)`
- 선택 상태를 보라색 점선으로 표현하지 않는다.
- 선택 상태는 배경색, 실선 테두리, 체크 아이콘으로 표현한다.

### 그림자

- 공공 서비스 특성상 그림자는 최소화한다.
- 기본 카드에는 그림자를 사용하지 않고 테두리로 구분한다.
- 플로팅 메뉴나 팝오버에만 약한 그림자를 사용한다.

---

## 8. 핵심 컴포넌트

다음 컴포넌트를 재사용 가능한 형태로 설계한다.

### 8.1 AppHeader

#### 구성

- 좌측 메뉴 버튼
- 중앙 서비스명
- 우측 더보기 버튼

#### 규격

- 높이 64px
- 아이콘 터치 영역 최소 44×44px
- 제목은 한 줄 유지
- 화면 상단 고정 여부는 현재 사용 흐름을 검토해 결정
- 모바일에서 좌우 버튼 때문에 제목이 치우쳐 보이지 않도록 중앙 정렬

### 8.2 SectionHeader

#### 구성

- 섹션 제목
- 선택적인 우측 `더보기` 링크

#### 규격

- 제목 18px, weight 700
- 더보기 13px, weight 600
- 더보기에는 최소 44px 높이의 터치 영역 제공

### 8.3 LiveReportCard

#### 구성

- 유기견 이미지
- 상태 배지
- 경과 시간
- 위치 또는 제보 제목

#### 규격

- 모바일 가로 스크롤 카드 너비 약 240px
- 모서리 16px
- 이미지 비율 고정
- 이미지에는 `object-fit: cover` 사용
- 카드 제목 최대 2줄
- 이미지가 없어도 레이아웃이 무너지지 않도록 fallback 제공

### 8.4 MapPanel

#### 구성

- 제주 지도
- 지역별 위치 마커
- 범례
- 필요한 경우 확대·축소 또는 전체 보기 기능

#### 규칙

- 최소 높이 180px
- 모서리 20px
- 배경은 밝은 청회색
- 범례는 12px 이상
- 범례 배경과 지도 간 대비 확보
- 마커는 색상뿐 아니라 형태 또는 아이콘으로도 구분
- 모바일 화면에서 지도 텍스트가 지나치게 작아지지 않도록 한다.

### 8.5 SortControl

#### 구성

- 제보 목록 개수
- 최근순
- 거리순

#### 규칙

- segmented control 또는 명확한 선택형 버튼으로 제작
- 선택 상태는 primary 배경과 흰색 글자
- 비선택 상태는 흰색 배경과 회색 테두리
- 높이 최소 40px
- 각 항목 터치 영역 최소 44px 확보

### 8.6 ReportListItem

#### 구성

- 접수 시간
- 견종 또는 특징
- 위치
- 상태 배지
- 사건 번호
- 거리
- 성별
- 세부 특징

#### 정보 위계

첫 번째 줄:

- 시간
- 핵심 특징
- 위치
- 상태

두 번째 줄:

- 사건 번호
- 거리
- 성별
- 외형 특징

#### 규격

- 카드 내부 여백 16px
- 카드 간격 12px
- 모서리 14~16px
- 보조 정보는 `text-secondary` 또는 `text-tertiary` 사용
- 한 카드에 너무 많은 색상을 사용하지 않는다.
- 긴 위치명은 말줄임 또는 두 줄 처리
- 전체 카드 또는 명확한 상세보기 버튼을 클릭 영역으로 사용

### 8.7 StatusBadge

#### 종류

- pending
- success
- transfer
- danger
- neutral

#### 규격

- 높이 26~28px
- 좌우 여백 10px
- 글자 12px, weight 600
- 원형 상태 점은 6px
- 아이콘을 사용한다면 12~14px
- 동일한 상태는 모든 화면에서 동일한 색과 라벨을 사용

### 8.8 ProcessTimeline

#### 단계

- 제보됨
- 확인 중
- 기관 전달
- 보호 / 반환

#### 규칙

- 현재 단계, 완료 단계, 예정 단계를 명확히 구분
- 완료 단계는 primary 또는 success
- 예정 단계는 neutral
- 진행 선과 단계 점의 정렬을 정확히 맞춘다.
- 모바일에서 단계명이 겹치지 않도록 균등 배치
- 처리 시간은 타임라인 아래 별도 정보 행으로 분리
- 파란색, 갈색, 보라색이 의미 없이 섞이지 않도록 상태 토큰 사용

### 8.9 Button

#### 종류

- Primary
- Secondary
- Tertiary
- Destructive
- Status Action

#### 공통 규격

- 높이 최소 44px
- 작은 운영자 버튼도 최소 40px 이상
- 좌우 여백 16px
- 모서리 10~12px
- 로딩, 비활성, 포커스 상태 제공
- 버튼 텍스트는 동작 중심으로 작성

#### 예시

- 보호 / 반환
- 기관 전달
- 제보 확인
- 상세 보기

### 8.10 DividerWithLabel

`운영자 처리 상태 변경`과 같이 구분선 사이에 제목이 들어가는 컴포넌트를 별도로 만든다.

#### 규칙

- 너무 진한 검은색 선을 사용하지 않는다.
- divider 색상 토큰 사용
- 제목과 선 사이에 12px 이상의 여백 확보

---

## 9. 정보 구조 개선

메인 화면의 정보 순서를 다음과 같이 정리한다.

1. 앱 헤더
2. 실시간 구조 현황
3. 지도 기반 현황
4. 제보 건수와 정렬
5. 제보 목록
6. 선택한 제보의 상세 처리 현황
7. 운영자용 처리 단계 변경

시민용 정보와 운영자용 기능이 한 화면에 함께 있을 경우 다음 중 적절한 방식을 선택한다.

- 운영자 영역을 별도 카드로 명확히 구분
- 운영자 권한이 있을 때만 영역 노출
- 모바일에서는 아코디언 또는 하단 패널로 분리
- PC에서는 우측 사이드 패널로 분리

시민이 보는 제보 정보와 관리자가 조작하는 버튼이 혼동되지 않도록 한다.

---

## 10. 반응형 규칙

모바일 우선으로 제작한다.

### Mobile: 360~430px

- 좌우 여백 20px
- 실시간 카드는 가로 스크롤
- 지도와 목록은 한 열
- 모든 버튼과 카드가 화면 너비에 맞게 배치
- 운영자 처리 영역은 별도 카드 또는 접이식 패널

### Tablet: 768px 이상

- 지도와 제보 목록을 2열로 배치할 수 있다.
- 실시간 카드는 2~3개 노출
- 운영자 처리 영역은 전체 너비 또는 우측 패널 사용

### Desktop: 1024px 이상

- 콘텐츠 최대 너비 1180~1200px
- 지도와 제보 목록을 좌우로 배치
- 선택된 제보의 처리 상태는 우측 상세 패널로 제공
- 카드가 불필요하게 넓어지지 않도록 `max-width` 지정
- 모바일 디자인을 단순 확대하지 말고 정보 구조를 재배치

---

## 11. 접근성

WCAG 2.1 AA 수준을 목표로 한다.

### 필수 조건

- 일반 텍스트 대비 4.5:1 이상
- 큰 텍스트 대비 3:1 이상
- 터치 영역 최소 44×44px
- 키보드 탐색 지원
- 모든 인터랙션 요소에 `focus-visible` 제공
- 이미지에 적절한 `alt` 텍스트 제공
- 상태를 색상만으로 전달하지 않는다.
- 아이콘 버튼에 `aria-label` 제공
- 현재 선택 상태에 `aria-selected` 또는 `aria-current` 사용
- 가로 스크롤 영역은 키보드와 스크린리더에서도 접근 가능하게 한다.
- 애니메이션은 `prefers-reduced-motion`을 지원한다.

---

## 12. 코드 구현 지침

먼저 기존 코드베이스를 확인하고 사용 중인 프레임워크와 스타일링 방식을 유지한다.

### 금지

- 기존 프레임워크를 임의로 교체
- 불필요한 UI 라이브러리 설치
- 기능이나 데이터 로직 삭제
- 모든 스타일을 한 파일에 작성
- 동일한 색상과 간격 값을 여러 곳에 하드코딩
- 인라인 스타일 남용
- 화면마다 별도의 상태 배지 구현
- 단순히 현재 화면을 픽셀 단위로 복제

### 구현 원칙

- 색상, 타이포그래피, 간격, 모서리, 그림자를 디자인 토큰으로 분리
- 컴포넌트는 variant 기반으로 제작
- 모바일과 PC가 같은 토큰을 공유하도록 구성
- 상태값과 화면 표현을 직접 연결할 수 있도록 semantic naming 사용
- 현재 프로젝트의 폴더 구조와 코딩 규칙을 따른다.
- TypeScript 프로젝트라면 컴포넌트 props 타입을 명시한다.
- 임시 데이터가 필요하면 현재 화면의 한국어 데이터를 기반으로 사용한다.
- Lorem ipsum과 의미 없는 영문 텍스트를 사용하지 않는다.

### Tailwind CSS를 사용 중이라면

- 색상, radius, spacing, fontSize를 theme에 등록
- 임의 값 사용을 최소화
- 반복되는 클래스는 컴포넌트 또는 variant로 추출

### 일반 CSS 또는 CSS Module을 사용 중이라면

- `:root`에 semantic CSS variables 정의
- 컴포넌트별 스타일 모듈 분리
- 상태는 `data-state` 또는 class variant로 관리

---

## 13. 생성할 결과물

다음 결과물을 순서대로 생성하라.

1. 현재 코드베이스와 화면 구조 분석
2. 디자인 시스템 적용 계획
3. 디자인 토큰 파일
4. 타이포그래피 및 전역 스타일
5. 공통 UI 컴포넌트
6. 메인 화면 컴포넌트 리팩터링
7. 모바일·태블릿·PC 반응형 처리
8. 접근성 상태 처리
9. 디자인 시스템 미리보기 화면 또는 컴포넌트 샘플 페이지
10. 변경된 파일 목록과 변경 이유 설명

가능하다면 다음 문서도 생성한다.

```text
docs/design-system.md
```

문서에 포함할 내용:

- 디자인 원칙
- 컬러 토큰
- 타이포그래피
- 간격과 모서리
- 상태 체계
- 컴포넌트 사용법
- 잘못된 사용 예
- 모바일과 PC 반응형 규칙

---

## 14. 최종 검수 기준

완료 후 다음 항목을 스스로 검수하라.

- 카드 반경과 테두리가 일관적인가?
- 상태 배지의 색상 의미가 모든 화면에서 동일한가?
- 12px 미만의 중요한 텍스트가 남아 있지 않은가?
- 버튼 터치 영역이 충분한가?
- 지도 범례를 쉽게 읽을 수 있는가?
- 시민용 정보와 운영자 기능이 구분되는가?
- 모바일 360px에서도 가로 넘침이 없는가?
- 393px 및 402px 화면에서 자연스럽게 표시되는가?
- PC에서 콘텐츠가 지나치게 늘어나지 않는가?
- 색상을 제거해도 상태를 이해할 수 있는가?
- 보라색 점선이나 개발용 표시가 남아 있지 않은가?
- 기존 기능과 데이터 흐름이 유지되는가?
- 전체 화면이 반려동물 쇼핑 앱이 아닌 신뢰도 높은 공공 서비스처럼 보이는가?

분석과 계획을 먼저 제시한 뒤 코드를 수정하라.

불확실한 부분은 임의로 새로운 기능을 만들지 말고 기존 코드와 화면 목적을 기준으로 가장 보수적인 방향을 선택하라.
