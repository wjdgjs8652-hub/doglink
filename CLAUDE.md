# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 프로젝트 성격

2026 제주 지역대학 연합 창업 캠프 출품작(팀 **성냥팔아요**)인 **유기견 발견·제보 연계 서비스**의 기획 문서 + 시연 프로토타입 저장소다.
빌드 시스템·패키지 매니저·테스트 러너가 없다. 프로토타입은 **의존성 없는 단일 HTML 파일**이며 브라우저로 파일을 직접 열면 실행된다.

```bash
start "" "mvp-prototype.html"
```

## 파일 구성과 권위 순서

| 파일 | 역할 |
|---|---|
| `MVP plan.docx` | **기능 정의의 원본.** Core/Supporting 기능, 유저 스토리, 완료 기준이 여기 있다. 기능 변경은 이 문서와 어긋나면 안 된다. |
| `Regional issues report.txt` | 문제 정의의 근거 통계(제주 유실·유기견 조사보고서 v2.0). 수치·주장을 쓸 때 출처. |
| `유기견앱_권한별기능_보안보강_메모.txt` | 권한별 공개 범위·개인정보 보강 요구사항. 팀 공유 문서라 저장소 밖(`~/OneDrive/문서/카카오톡 받은 파일/`)에 있다. 권한·위치 공개 정책을 바꿀 때 근거. |
| `doglink_design_system_v2.md` | **디자인의 단일 진실 공급원(SSOT).** 서비스명 doglink, 컬러·타이포 토큰, 트리아지 체계, 컴포넌트 규격. 시각·UX 변경은 이 문서를 따른다. |
| `design-system.html` | 토큰·컴포넌트 정적 미리보기 페이지 (문서 §10 생성 순서 ⑧). |
| `mvp-prototype.html` | 위 문서의 핵심 기능 3가지를 구현한 시연용 단일 파일 프로토타입. |
| `doglink_운영자_플로우차트.md` | **운영자 콘솔 요구사항 원본** — 상태 머신·불변 규칙(응급 상시 인지, 3클릭, AI 제안·사람 확정, 감사 추적). |
| `operator/` · `shared/` · `docs/operator-*.md` | 기관용 운영자 콘솔(무빌드 정적 HTML+JS, `operator/index.html`을 브라우저로 직접 열어 실행)과 시민 서비스 공용 디자인 자산, 구현·mock 문서. |
| `public/admin/` · `sync_admin.py` | Vercel 배포용 운영자 콘솔 사본 — 배포 URL의 `/admin`에서 서빙. **생성물이므로 직접 편집 금지**: `operator/`·`shared/`를 고친 뒤 `python sync_admin.py`로 재생성한다. |
| `build_deck.py` / `qa_deck.py` | 발표 덱 생성기와 기하 QA. `성냥팔아요_발표자료.pptx`는 **생성물이므로 직접 편집하지 말고 스크립트를 고쳐 다시 만든다.** |

`.docx`는 Read 툴로 못 읽는다. 압축을 풀어 `word/document.xml`의 `w:t` 노드를 이어붙여야 한다 (한글 때문에 출력은 반드시 UTF-8로 파일에 쓸 것 — Windows 콘솔은 cp949라 `print`가 깨진다).

## 프로토타입 구조 (`mvp-prototype.html`)

단일 IIFE, 프레임워크·외부 리소스·네트워크 요청 없음. 이미지는 인라인 SVG, QR은 canvas로 그린 시연용 패턴, 지도는 손으로 그린 제주도 SVG path에 좌표 마커를 얹은 것이다(실좌표가 아닌 `viewBox 720×380` 화면 좌표계이며 `dist()`가 ×0.11로 km 환산).

문서의 기능 3가지가 그대로 3개 탭 = 3개 `<section class="screen">`에 대응한다.

1. **Core — QR 기반 AI 보조 무설치 제보** (`#s1`): `flow` 객체 하나가 step 0→4 상태를 들고 있고 `renderPhone()`이 매 step마다 `#phone`의 innerHTML을 통째로 교체한다. 따라서 **화면을 다시 그리기 전에 입력값을 먼저 읽어야 한다** (`runAI()` 참고 — 순서를 바꾸면 폼 값이 사라진다).
2. **Supporting 1 — 제보 지도·상태** (`#s2`): `view`가 `public`/`admin`을 가르고, 공개 화면에서는 정확 좌표 대신 halo(반경 300m)만 렌더한다. 이 구분은 문서의 개인정보 요구사항이므로 임의로 없애지 말 것. 여기에 **처리시간 계측**이 얹혀 있다(아래 참고).
3. **Supporting 2 — 중복·실종 매칭** (`#s3`): `candidatesFor()`가 털색·무늬·체격·거리·시간차·목줄을 가중 합산해 상위 3건만 반환한다. **AI는 확정하지 않는다** — 연결/중복 병합/무관은 `decisions`에 사람이 기록하는 구조를 유지해야 한다.

상태는 전역 배열 `reports`/`missing`/`logs`에만 있고 영속화하지 않는다. 변경 후에는 관련 `render*()`를 직접 호출한다(반응형 바인딩 없음).

### 권한 모델 (`role` / `project()` / `publicLoc()`)

역할은 `public`(일반 시민) / `owner`(제보자 본인) / `agency`(기관 담당자) 3가지이며 상단 권한 바가 전역으로 바꾼다. 예전의 공개/운영자 2단 토글을 대체한 것이다.

**핵심 규칙: 화면에서 가리지 말고 데이터에서 지운다.** `project(r, role)`이 역할별로 서버가 내려줄 필드만 담은 객체를 만들고, 렌더러는 그 결과만 쓴다. 시민 응답에는 `정확좌표`·`상세주소`·`목격이력`·`신고자`·`내부메모`가 아예 없다(13개 키 vs 기관 19개). "이 권한이 받는 데이터" 버튼이 그 JSON과 제거된 필드를 그대로 보여주므로, **`project()`를 우회해 원본 `r`에서 좌표를 읽어 렌더하면 이 시연이 거짓말이 된다.**

`publicLoc(r)`이 위험도별 공개 범위를 정한다 — `normal` 300m, `resident` 동 단위, `urgent`는 **기관 확인(status≥1) 전까지 지도에서 제외**, 종결(status 3)은 공개 지도에서 내린다. 공개 마커는 중심을 흔들어(`jx/jy`) 원의 중심이 곧 좌표가 되지 않게 한다. 범위 원은 실제 축척이 아니다(1 map unit ≈ 110m라 300m는 3px이 되어 보이지 않는다) — 가독성용 과장이다.

거리도 공개 데이터에서는 `distLabel()`로 범위화한다(1km 이내 / 약 N km / 10km 이상). 정확한 km는 역산 단서다.

기관이 좌표를 보려면 `revealed`에 들어가야 하고, 그러려면 **열람 사유를 고르는 것이 강제**된다(`#reason`). 열람은 `access[]`에 기록되어 "위치 열람 기록" 카드에 남는다. 매칭 결정도 같은 방식으로 사유가 필수이며 `decisions[key]`에 담당자·시각·이전 상태를 저장해 되돌릴 수 있다.

운영 지표(KPI 4타일)는 `agency`에서만 보인다. 시민·제보자에게는 `renderMyStatus()`의 "내 제보 처리 안내"만 나간다.

### 처리시간 계측 (`stamps` / `SLA` / `slaOf()`)

각 제보는 `stamps[0..3]`을 갖는다 — 상태 `i`에 **진입한 시각**, 미도달이면 `null`, `stamps[0]`은 항상 접수 시각이다. 운영자가 상태를 올리면 건너뛴 단계까지 같은 시각으로 채우고, **되돌리면 이후 stamps를 `null`로 무효화**한다(안 그러면 유령 소요시간이 남는다).

`SLA = [0, 30, 120, 1440]`(분)은 **접수 시각 기준 누적** 목표이며 기관 협의 전 가정치다. 화면에 "시연용 가정치"라고 밝혀 둔 문구를 지우지 말 것.

**여기서 보여주는 숫자는 기존 통계가 아니라 서비스가 수집하는 계측값이다.** `Regional issues report.txt` 12-2절이 "신고 후 담당자 확인·출동까지 걸리는 평균 시간"을 *아직 확인되지 않은 항목*으로 분류하고 12-4절이 이를 개발 전 필수 측정 지표로 지목하기 때문이다. 이 화면을 "처리가 오래 걸린다"는 주장으로 바꾸면 근거자료와 어긋난다.

경과시간은 계속 흐르므로 30초 인터벌이 `renderKpi()`와 활성 탭을 다시 그린다. **입력 중인 `#s1`은 건드리지 않는다** — 다시 그리면 폼 값이 날아간다.

### 디자인 시스템 (doglink v2 — `doglink_design_system_v2.md`가 SSOT)

색·타이포·컴포넌트 규격은 전부 SSOT 문서에서 온다. 임의 색·임의 간격을 만들지 말 것. 요약:

- **역할 고정 매핑** (어느 화면에서도 불변): 파랑 `#526ED8`=행동, 빨강=응급(트리아지 전용), 주황=출동/대기, 초록=완료, 보라=기관 전달, 회색=부정/비활성.
- **트리아지 ≠ 처리 상태.** AI 판정(응급/출동/부정/분석 중)은 `TriageBadge`(`.triage[data-state]`), 처리 4단계는 `StatusBadge`(`.badge .b-1..4`)로만 표현한다. 혼용 금지. 트리아지는 항상 아이콘+한글 라벨+색 3요소.
- **solid 응급색(`#D32F2F`)의 허용처는 3곳뿐**: 응급 배너, EmergencyConfirmSheet, 지도 응급 마커. 그 외 사용은 경고 피로.
- **지도 마커는 색+형태 이중 부호화**: 응급=원, 출동=사각, 부정=작은 점, 종결=원+체크. 마커 4색 all-pairs 검증값: normal 최저 17.6(통과), CVD 최저 쌍은 응급-종결 7.8(floor 대역)이나 체크 글리프가 보조 채널. 색을 바꾸면 재계산할 것.
- **점선 테두리 절대 금지** (지연 링·진행 중 세그먼트도 실선/빗금). **다크 모드 없음** — v2 문서가 라이트 팔레트만 정의하므로 만들지 않았다. 다크를 추가하려면 SSOT 문서 개정이 먼저다.
- 예전 자체 검증 팔레트(`#1f6feb·#d97706·#b5179e·#0f7b52`)는 v2 토큰으로 대체됐다. `--st1..4`는 이제 처리 상태 텍스트 톤(회/주황/보라/초록)이며 타임라인 세그먼트에 쓰인다.

### 트리아지 플로우 (`TRIAGE` / `analyze()` / `openEmergencySheet()`)

`analyze()`가 표준 카드와 함께 `triage`(emergency/dispatch/negative)와 `triBasis`(근거 문장)를 산출한다. **판정은 단정형으로 쓰지 않는다** ("응급으로 판단됩니다" O / "응급입니다" X). TriageResultCard에는 근거 요약 + "AI 판단이며 담당자가 최종 확인합니다" + 판정 수정 진입점("판정이 다른 것 같아요")이 항상 붙는다.

응급 판정 확정 시 `openEmergencySheet()`가 열린다 — 카운트다운 10초(+30초 연장 버튼, `aria-live="assertive"`), **"응급이 아니에요" 중단 버튼은 신고 버튼과 동일 크기·동일 위계**(다크패턴 금지, 검수에서 두 버튼 rect 크기가 같아야 한다). 자동 신고는 `saveReport(true)` — status 2로 직행하며 `stamps[0..2]`에 같은 시각이 들어가고 **초 단위(`hhmmss`)로 표시**해 자동화가 실제 작동했음을 보여준다(B2G 시연 포인트). 처리 단계는 여전히 4단계다 — 문서의 6단계 타임라인은 `stamps`/SLA 계측과 충돌해서 "AI 판정"을 표시 전용 주석으로만 넣었다.

기관 큐(`renderNewList`)는 트리아지 우선(응급→출동→부정)→최근순 정렬에 응급 행 좌측 액센트 바, ↑↓/Enter 키보드 탐색. KPI 첫 타일은 **응급 미처리 카운터**(0건 아니면 응급색)다.

### 모의 AI

`analyze()`는 LLM이 아니라 정규식 규칙이다. 규칙을 넓힐 때 한국어 부분 문자열 오탐에 주의할 것 — 예전에 `/피/`가 "사람을 **피**해서"를 부상으로, `/서/`가 "피해**서**"를 서쪽 이동으로, `/주인/`이 "주인**은 안 보입니다**"를 보호자 동반(부정 판정)으로 잡은 적이 있다. 조사·어미까지 포함한 긍정 패턴을 쓰고, 부재 서술(`안/없`)을 별도로 걸러낸다.

## 운영자 콘솔 (`operator/`, `shared/`)

기관 담당자용 관제 콘솔. 시민 프로토타입과 완전히 분리된 별도 앱이며 로그인 후에만 접근된다
(`#/login` → `#/queue` → `#/statistics` → `#/design-system`, 해시 라우터).
빌드 없음 — 일반 `<script>` 다중 파일 구조라 file://에서 그대로 동작한다.

- **상태 머신이 단일 소스다.** `operator/js/state-machine.js`의 `ALLOWED_TRANSITIONS`
  (접수됨→확인 중→기관 전달/출동→보호/반환→종결, 부정 종결 분기)에서 ActionBar 버튼과
  단축키(E/P/R)가 파생된다. 버튼을 수동으로 추가하지 말 것. UI 비활성화와 별개로
  `services.requestTransition`이 데이터 계층에서 재검증한다.
- **모든 변경은 감사 로그와 함께 원자 처리**(`writeAudit`가 같은 함수 안에서 호출). 삭제 기능은
  어디에도 없다. AI 판정 번복은 사유 필수 + 원 판정(`triage.originalType`) 보존, 매칭은
  연결/병합(미리보기 필수)/무관 3종 모두 사람이 확정한다 — AI 자동 확정 경로를 만들지 말 것.
- **시민 연동 데이터 계약**: `services.citizenView()`가 8단계를 시민 4단계로 투영하며 정확
  좌표·상세 주소를 **필드에서 제거**한다(프로토타입의 `project()`와 같은 원칙). 상태 변경 성공
  토스트에는 "시민 화면에 반영됨"을 표기한다.
- **응급 불변 규칙**: 응급 미처리(=트리아지 응급 & 접수됨)는 전역 카운터(필터와 무관, 15분+ 시
  최장 경과 병기, `aria-live="assertive"`) + 큐 최상단 고정 + 자동 소멸하지 않는 토스트로 상시
  인지된다. 토스트에 타이머를 달지 말 것.
- **전부 mock adapter다** (인증·실시간·지도·알림·PDF — `docs/operator-mock-api.md`에 연동
  필요 지점 정리). 신규 제보 유입 시연은 계정 메뉴의 "시연 도구"에서 수동 주입한다.
  mock 계정: `jeju.kim`/`jeju.park`/`jeju.lee` + `doglink-demo`, 총괄 `admin` + `doglink-super`(권한 등급 동일).
- **공용 자산은 `shared/`** (tokens.css·components.css·components.js — TriageBadge·StatusBadge·
  ProcessTimeline·Dialog·Toast·날짜 유틸). `mvp-prototype.html`은 단일 파일 속성을 지키기 위해
  내부 복사본을 유지한다 — 규격을 바꿀 땐 양쪽과 SSOT 문서를 함께 본다.
- 검증은 시민 프로토타입과 동일하게 브라우저 수동 검수: 로그인 → 큐 정렬 → 상세 →
  번복(사유 필수) → 매칭 병합 미리보기 → 상태 변경(자동 배정 고지) → 종결(분류 필수) →
  통계(정의 병기·CSV) → 키보드(↑↓/Enter/Esc/E/P/R/A,/,?) → 360px 넘침 0 → 콘솔 에러 0.

## 검증 방법

테스트 스위트가 없다. 변경 후에는 브라우저에서 `javascript_tool`로 흐름을 직접 굴려 확인한다: 스캔 → 사진·GPS·서술 입력 → `#submit` → (1.1초 후) TriageResultCard 판정 확인 → `#confirm` → (응급이면) EmergencyConfirmSheet의 카운트다운·연장·중단 → 접수번호 발급 → 지도 마커 반영 → KPI 5타일(응급 카운터 포함) 갱신 → 매칭 후보 3건 → 상태 변경이 공개 화면과 타임라인에 반영 → 되돌리면 stamps 무효화. 트리아지 세 갈래(샘플 1=출동, 샘플 2=응급, "주인이 데리고" 서술=부정)가 모두 나오는지, 응급 시트의 두 버튼 크기가 같은지, 360px에서 가로 넘침이 없는지, 콘솔 에러 0을 확인한다. 다크 모드는 없다(라이트 전용).

### 발표 덱 (`build_deck.py`)

`python-pptx`로 13장을 생성한다. 이 환경에는 **node·LibreOffice가 없어 슬라이드를 이미지로 렌더할 수 없다.** 그래서 시각 QA 대신 두 가지를 계산으로 돌린다.

```bash
python build_deck.py   # WARNINGS 0 이어야 함 (텍스트가 박스를 넘치는지)
python qa_deck.py      # problems 0 이어야 함 (텍스트 겹침·슬라이드 이탈·여백)
```

`build_deck.py`의 `fit()`이 한글 1em·라틴 0.53em으로 폭을 추정해 오버플로를 잡고, `qa_deck.py`가 텍스트 박스끼리의 겹침과 7.5in 밖 이탈을 잡는다. 제목에 `sub`가 있으면 제목 박스를 1줄 높이(0.72in)로 묶어 두었다 — 2줄로 넘치면 부제와 겹치므로 `fit()`이 먼저 잡게 하려는 것이다.

스킬의 `validate.py`는 한국어 Windows에서 cp949로 XML을 읽다 실패한다. `PYTHONUTF8=1`을 주면 통과한다.

권한 회귀는 3역할 × (지도·목록·상세·서버응답)으로 돌린다. 특히 **시민/타인 사건에서 `33.xxxx, 126.xxxx` 패턴이 DOM에 등장하면 실패**다.

`.map-wrap svg{width:100%;display:block}`가 **지도 컨테이너 안의 모든 svg**를 잡는다. 범례 같은 작은 아이콘을 svg로 넣으면 62px로 부풀어 터진다 — CSS 스와치(`.map-legend i`)를 쓸 것.
