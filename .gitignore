# React/Vite 앱
node_modules
dist
*.local
.env
.env.*
!.env.example
.vercel

# 생성물 — build_deck.py로 재생성 (파일 잠금 문제도 있어 추적 제외)
성냥팔아요_발표자료.pptx

# 로컬 전용 설정
.claude/settings.local.json

# Office 임시/잠금 파일
~$*
*.tmp
desktop.ini

# 빌드 시 환경 변수에서 생성 (scripts/gen-admin-config.mjs) — 키 커밋 금지
operator/js/runtime-config.js
public/admin/js/runtime-config.js
